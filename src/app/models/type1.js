class Type1Input {
    constructor(dataset, userId,autocomplete,pat,editMode) {
      this.dataset = dataset;
      this.userId = userId;
      this.autocomplete = autocomplete;
      this.pat = pat;
      this.editMode=editMode
    }
  }
  export default Type1Input;